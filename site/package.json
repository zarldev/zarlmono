{
  "name": "zarlmono-docs",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4322",
    "test": "playwright test",
    "test:install": "playwright install chromium",
    "test:install:ci": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "@astrojs/starlight": "0.42.3",
    "astro": "7.3.4"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0"
  }
}
